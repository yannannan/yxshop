import{i as e,t}from"./setup-Q_JED0_3.js";async function n(){await t();let[n,r,i,a,o,s,c,l,u]=await Promise.all([e.DB.prepare(`SELECT * FROM products ORDER BY sort_order,id`).all(),e.DB.prepare(`SELECT * FROM users ORDER BY id DESC`).all(),e.DB.prepare(`SELECT o.*,p.name AS product_name,p.category,p.subcategory,u.phone,u.email FROM orders o JOIN products p ON p.id=o.product_id JOIN users u ON u.id=o.user_id ORDER BY o.created_at DESC`).all(),e.DB.prepare(`SELECT c.*,p.name AS product_name,p.price AS product_price FROM credentials c JOIN products p ON p.id=c.product_id ORDER BY c.id DESC`).all(),e.DB.prepare(`SELECT ca.id,ca.credential_id,ca.order_id,ca.status AS assignment_status,ca.assigned_at,ca.released_at,u.phone,u.email,COALESCE(o.status,'deleted') AS order_status,COALESCE(NULLIF(ca.product_name,''),op.name,cp.name,'—') AS product_name,COALESCE(NULLIF(ca.category,''),op.category,cp.category,'—') AS category,COALESCE(NULLIF(ca.subcategory,''),op.subcategory,cp.subcategory,'—') AS subcategory,COALESCE(NULLIF(ca.billing_cycle,''),op.billing_cycle,cp.billing_cycle,'once') AS billing_cycle FROM credential_assignments ca JOIN users u ON u.id=ca.user_id LEFT JOIN orders o ON o.id=ca.order_id LEFT JOIN products op ON op.id=o.product_id LEFT JOIN credentials c ON c.id=ca.credential_id LEFT JOIN products cp ON cp.id=c.product_id ORDER BY ca.assigned_at DESC`).all(),e.DB.prepare(`SELECT id,product_id,name,type,amount,status,CASE WHEN image_url IS NULL OR image_url='' THEN '' ELSE '1' END AS image_url FROM payment_qrs ORDER BY id DESC`).all(),e.DB.prepare(`SELECT c.*,p.name AS parent_name FROM categories c LEFT JOIN categories p ON p.id=c.parent_id ORDER BY c.parent_id,c.sort,c.id`).all(),e.DB.prepare(`SELECT * FROM notification_settings ORDER BY type`).all(),e.DB.prepare(`SELECT * FROM email_config WHERE id=1`).all()]);return Response.json({products:n.results,users:r.results,orders:i.results,credentials:a.results,credentialAssignments:o.results,qrs:s.results,categories:c.results,notificationSettings:l.results,emailConfig:u.results[0]||null},{headers:{"Cache-Control":`no-store, max-age=0, must-revalidate`,"CDN-Cache-Control":`no-store`}})}export{n as GET};