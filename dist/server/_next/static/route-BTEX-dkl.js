import{i as e,t}from"./setup-Q_JED0_3.js";import{t as n}from"./order-notifications-DqVSsIlA.js";async function r(r){if(await t(),!e.ORDER_REPLY_TOKEN||r.headers.get(`x-order-reply-token`)!==e.ORDER_REPLY_TOKEN)return Response.json({message:`回复验证失败`},{status:401});let i=await r.json(),a=`${i.orderId||``}\n${i.subject||``}\n${i.text||``}`,o=i.orderId||a.match(/QX\d{11}_[A-Z0-9]+/i)?.[0],s=n[String(i.status||a.match(/待支付|已支付|待发货|已发货|已作废/)?.[0]||``).trim()];if(!o||!s)return Response.json({message:`请提供订单号和有效回复状态`},{status:400});let c=await e.DB.prepare(`SELECT credential_id FROM orders WHERE id=?`).bind(o).first();if(!c)return Response.json({message:`订单不存在`},{status:404});let l=new Date().toISOString();if(s===`delivered`)await e.DB.prepare(`UPDATE orders SET status=?,delivered_at=COALESCE(delivered_at,?),updated_at=? WHERE id=?`).bind(s,l,l,o).run();else{if(c.credential_id){await e.DB.batch([e.DB.prepare(`UPDATE credential_assignments SET status='released',released_at=? WHERE order_id=? AND status='active'`).bind(l,o),e.DB.prepare(`UPDATE orders SET credential_id=NULL WHERE id=?`).bind(o)]);let t=await e.DB.prepare(`SELECT COUNT(*) AS total FROM credential_assignments WHERE credential_id=? AND status='active'`).bind(c.credential_id).first();Number(t?.total||0)||await e.DB.prepare(`UPDATE credentials SET status='available' WHERE id=? AND status='used'`).bind(c.credential_id).run()}await e.DB.prepare(`UPDATE orders SET status=?,delivered_at=NULL,updated_at=? WHERE id=?`).bind(s,l,o).run()}return Response.json({ok:!0,orderId:o,status:s})}export{r as POST};